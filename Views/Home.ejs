<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%='Home Page' %></title>
    <style>
      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .navbar {
        background: linear-gradient(90deg, #4a90e2, #357abd);
        padding: 12px 30px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.15);
      }
      .navbar a {
        color: white;
        text-decoration: none;
        margin: 0 18px;
        font-weight: 600;
        font-size: 1.1rem;
        position: relative;
        transition: color 0.3s ease;
      }
      .navbar a:hover {
        color: #ffb347;
      }
      .navbar a::after {
        content: "";
        position: absolute;
        width: 0;
        height: 2px;
        background: #ffb347;
        left: 0;
        bottom: -5px;
        transition: width 0.3s ease;
      }
      .navbar a:hover::after {
        width: 100%;
      }
      .brand {
        color: white;
        font-size: 28px;
        font-weight: 700;
        letter-spacing: 2px;
        cursor: default;
        user-select: none;
        text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.3);
      }

      .main-content {
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 60px 20px;
        text-align: center;
        color: #2e3a59;
      }

      .main-content h1 {
        font-size: 3rem;
        margin-bottom: 20px;
        font-weight: 700;
        background: linear-gradient(90deg, #ff6a88, #ffb347);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: gradientText 5s ease infinite alternate;
      }

      .main-content p {
        font-size: 1.25rem;
        color: #555;
        max-width: 400px;
        line-height: 1.6;
        background-color: #ffffffcc;
        padding: 20px 30px;
        border-radius: 15px;
        box-shadow: 0 8px 25px rgba(255, 106, 136, 0.2);
        transition: box-shadow 0.3s ease;
      }
      .main-content p:hover {
        box-shadow: 0 12px 40px rgba(255, 106, 136, 0.4);
      }

      @keyframes gradientText {
        0% {
          background-position: 0% 50%;
        }
        100% {
          background-position: 100% 50%;
        }
      }

      @media (max-width: 600px) {
        .main-content h1 {
          font-size: 2.2rem;
        }
        .main-content p {
          font-size: 1rem;
          max-width: 90%;
          padding: 15px 20px;
        }
        .navbar a {
          margin: 0 10px;
          font-size: 1rem;
        }
        .navbar {
          padding: 12px 15px;
        }
        .brand {
          font-size: 22px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar">
      <div class="brand"><%='Apna Ghar' %></div>
      <div>
        <a href="/">Home</a>
        <a href="/Add-Home">Add Home</a>
        <a href="/ContactUs">Contact Us</a>
      </div>
    </nav>

    <!-- Main Content -->
    <% if(homes.length === 0){ %>

    <div class="main-content">
      <h1><%= 'Welcome to Home Page' %></h1>
      <p><%= 'There is no Home Available Now.' %></p>
    </div>

    <% } else { %>
    <h1 style="text-align: center; color: #333">Available Homes</h1>
    <ul
      style="
        list-style: none;
        padding: 0;
        margin: 0 auto;
        max-width: 1000px;
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: center;
      "
    >
      <% homes.forEach(home => { %>
      <li
        style="
          background-color: #fff;
          border: 1px solid #ddd;
          border-radius: 8px;
          width: 280px;
          padding: 15px;
          box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
        "
      >
        <h2 style="margin: 0 0 10px; color: #007bff; font-size: 20px">
          <%= home.homeName %>
        </h2>
        <p style="margin: 5px 0"><strong>Price:</strong> ₹<%= home.price %></p>
        <p style="margin: 5px 0">
          <strong>Location:</strong> <%= home.location %>
        </p>
        <p style="margin: 5px 0">
          <strong>Rating:</strong> ⭐ <%= home.rating %>
        </p>
        <% if (home.photo) { %>
        <img
          src="<%= home.photo %>"
          alt="Photo of <%= home.homeName %>"
          style="
            width: 100%;
            height: 160px;
            object-fit: cover;
            border-radius: 4px;
            margin-top: 10px;
          "
        />
        <% } else { %>
        <img
          src="https://via.placeholder.com/280x160?text=No+Image"
          alt="No Image"
          style="
            width: 100%;
            height: 160px;
            object-fit: cover;
            border-radius: 4px;
            margin-top: 10px;
          "
        />
        <% } %>
      </li>
      <% }) %>
    </ul>
    <% } %>
  </body>
</html>
